version: "3.1"

rules:
  - rule: Say goodbye anytime the user says goodbye
    steps:
      - intent: goodbye
      - action: utter_goodbye

  - rule: Say 'Goodbye and send contack info' anytime the user end the conversation
steps:
  - intent: bot_challenge
  - action: utter_iamabot
